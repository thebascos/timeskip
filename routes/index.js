const express = require('express');
const router = express.Router();

router.get('/', function (_req, _res) {});

router.post('/login', function (_req, _res) {});

router.post('/signup', function (_req, _res) {});

module.exports = router;
